program fibonacci;

var n, i : int;

function fib(n: int) : int {
    if (n < 1 || n == 1) {
        return(n);
    }
    else {
        return(fib(n-1) + fib(n-2));
    }
}

{
    for (i = 0 to 11) {
        print(fib(i));
    }
    print("done");
}
