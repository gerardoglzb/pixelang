program factorial_recursivo;

var a, b, c : int;
var arr : int[5];
var i : int;

function fact(x : int) : int {
    var y : int;
    if (x < 1 || x == 1) {
        return(1);
    }
    y = x - 1;
    return(x * fact(y));
}

{
    print(fact(6-1));
    print(-5.4);
}
