program clones;

var original : image;
var i, n : int;
var filename, decision : string;

{
    folder = sinput("What folder are your images located in? (Leave blank for root directory)");
    filename = sinput("What image would you like to add filters to?");
    original.open(folder + filename);
    decision = sinput("Would you like to apply 'grayscale' effect?" (Type 'Y' for yes));
    if (decision == "Y") {
        original.grayscale();
    }
    filename = sinput("What would you like to save your new image as?");
    ooriginal.save(folder + filename);
}