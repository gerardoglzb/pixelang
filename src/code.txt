program test;

var a, b, c : int;
var arr : int[5];
var i : int;

function fill(am: int, a : int) : void {
    var i : int;
    for (i = 0 to am) {
        arr[i] = a * a * i;
    }
}

{
    a = 3;
    i = 0;
    fill(4, 2);
    while (i < 5) do {
        print(arr[i]);
        i = i + 1;
    }
}
