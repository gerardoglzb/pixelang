#include <iostream>

/*
All the possible datatypes.
*/
enum Type {
    INT_,
    FLOAT_,
    BOOL_,
    STRING_,
    VOID_,
    IMAGE_,
};

/*
The two values a boolean could have.
*/
enum Boolean {
    FALSE_,
    TRUE_,
};